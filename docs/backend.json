{
  "entities": {
    "Submission": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Submission",
      "type": "object",
      "description": "Represents a form submission from the frontend.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the submission."
        },
        "pageId": {
          "type": "string",
          "description": "The ID of the page where the form was submitted."
        },
        "pageName": {
          "type": "string",
          "description": "The name of the page where the form was submitted. Used to categorize submissions."
        },
        "formData": {
          "type": "object",
          "description": "Object containing the submitted form data."
        },
        "submittedAt": {
          "type": "string",
          "description": "The date and time when the form was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "pageId",
        "pageName",
        "formData",
        "submittedAt"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the LandEase application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "pages": {
            "type": "array",
            "description": "An array of page references owned by the user.",
            "items": {
                "type": "object",
                "properties": {
                    "pageId": { "type": "string" },
                    "pageName": { "type": "string" }
                },
                "required": ["pageId", "pageName"]
            }
        }
      },
      "required": [
        "id",
        "email"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles, indexed by their unique Firebase Auth ID.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user, matching their Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/submissions/{submissionId}",
        "definition": {
          "entityName": "Submission",
          "schema": {
            "$ref": "#/backend/entities/Submission"
          },
          "description": "Stores form submissions for a specific user. Each submission is associated with a page.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the page and submissions."
            },
            {
              "name": "submissionId",
              "description": "The unique ID of the form submission."
            }
          ]
        }
      }
    ]
  }
}
